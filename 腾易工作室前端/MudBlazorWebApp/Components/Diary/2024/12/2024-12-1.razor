@page "/Diary"
@using MudBlazorWebApp.Components.Layout
@layout DiaryLayout

<MudText>为什么错误</MudText>
@code {
    [Parameter] public Guid Guid { get; set; }  // 接受来自 URL 的 Guid

    private Diary? selectedDiary;

    protected override void OnInitialized()
    {
        selectedDiary = DiaryData.diary2024Y12M.FirstOrDefault(d => d.Guid == Guid);
    }
}
